{{- if .Values.postgresql.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "atuin.postgresql.fullname" . }}
  labels:
    {{- include "atuin.postgresql.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: postgresql
      protocol: TCP
      name: postgresql
  selector:
    {{- include "atuin.postgresql.selectorLabels" . | nindent 4 }}
{{- end }}
